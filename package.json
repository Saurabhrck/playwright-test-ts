{
  "name": "playwright-test-suite",
  "version": "1.0.0",
  "author": "LambdaTest",
  "engines": {
    "node": ">=12.0.0"
  },
  "description": "Sample project to run playwright tests using playwright runner on LambdaTest platform",
  "scripts": {
    "pretest": "sudo npx playwright install-deps && sudo npx playwright install",
    "test": "npx playwright test --config=./playwright.config.ts --project=\"chrome:latest:Windows 10@lambdatest\""
  },
  "dependencies": {
    "@playwright/test": "1.24.2",
    "dotenv": "^16.0.2",
    "playwright": "1.24.2"
  },
  "devDependencies": {
    "@types/node": "18.7.18",
    "typescript": "4.8.3"
  }
}
